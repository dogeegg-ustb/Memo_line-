# 视口切割边 ↔ 工作区切割边对应：强约束修改契约

> 本文是 `screen_canvas_transform` 的**增量强约束契约**，专门修正  
> **导航器缩略图内红视口边的方位指派（L/T/R/B / `workspace_edge`）**。  
> 实现 MUST 遵守 `MUST / MUST NOT / SHOULD / MAY`。  
> 本文**不推翻**现有：截图、ROI 锚点、C-II 缩略图检测、OCR 读数、直接工作区路径、Transform 求解矩阵权威。  
> 本文 ONLY 约束：视口边角色如何由**切割对应**确定，以及角在本路径中的用途。

---

## 1. 目标

把「红视口边方位」从：

```text
ThumbnailRoi 局部组内排序（最左→L / 最右→R …）+ ROI 中线兜底
```

改为：

```text
工作区切割边 ↔ 视口切割边（谁切的就对应谁）
+ 已知显示角仅用于逆向旋转以加速形状对齐
+ 一对（或四对）切割对应 → 传播 / 套用 0° 下的 L/T/R/B
```

覆盖层「左/右/上/下」标签 MUST 反映上述对应结果，MUST NOT 再表示「缩略图 ROI 内相对更靠边」。

---

## 2. 非目标（明确保留）

以下能力 MUST 保留，不得因本修改而删除或改语义：

1. **直接工作区路径**：工作区画布四边完整且不歧义时，MUST 仍可跳过红框方位依赖（既有 `UsedDirectWorkspacePath` / 0.0 语义）；
2. **红段观测搜索域**：红像素/红段检测 MUST 仍以 `NavigatorThumbnailRoi` 为搜索域；MUST NOT 改写 ThumbnailRoi 锚点；
3. **成组 / Pattern 补全**（几何补矩形）MAY 保留，但**边角色标签** MUST 服从本契约的切割对应，MUST NOT 再由组内排序单独定案；
4. **求解旋转权威**：Transform 求解中几何旋转与 OCR 旋转的既有权威关系 MUST NOT 被本文悄悄改写；本文仅约束**边对应路径**里角的用法（见 §6）。

---

## 3. 问题陈述（当前违规）

### 3.1 当前行为

`viewport_frame.cpp` 中 `AssignGroupWorkspaceEdges`：

- 组内竖边按局部 `coord` 排序 → 最左 L、最右 R；
- 组内横边同理 → 最上 T、最下 B；
- 单边时用 **ThumbnailRoi 中线** 改指派。

`WorkspaceCanvasRelation.canvas_crop_sides` / `occluded_canvas_edges` 虽已计算，但对 `workspace_edge` **零约束**。

### 3.2 后果

在工作区被 ROI 边裁切、导航器红框仅部分穿插缩略图显示画布、或显示角非 0°（例如 180°）时：

- 屏幕「工作区下边在切」应对应「穿插缩略图显示画布的那条红切割边」（常为红框上沿），而非「红框下边」或「ROI 更靠下的边」；
- 局部排序 / 中线会系统性地标错 L/T/R/B，覆盖层与后续语义一并错误。

### 3.3 根因一句话

**把「缩略图局部几何位置」当成了「工作区边角色」；正确角色应由切割同构 +（可选）逆向旋转后的 0° 标签给出。**

---

## 4. 规范定义（禁止混用）

以下术语在实现、日志、测试中 MUST 使用同一语义。

### 4.1 工作区显示画布 / 导航器缩略图显示画布

在对应 ROI（`WorkspaceRoi` / `NavigatorThumbnailRoi`）内：

- MUST 将「工作区背景」排除后得到的前景视为**显示画布**；
- **弱几何修正**仅作用于**用于排除的工作区背景形状**：规则 MUST 固定、可复现；  
  MUST NOT 限制背景块的几何形态、数量、排布；  
  MUST NOT 对显示画布强加「必须是矩形 / 单连通 / 特定长宽比」等强形状先验（标准可分几何即可）。

### 4.2 工作区切割边

- 定义：将**完整画布**遮蔽成**不完整工作区显示画布**的边缘；
- MUST 是 **WorkspaceRoi 的边缘**（可贴 ROI 边判定）；
- 可理解为对显示画布的「切割」。

无任何工作区切割边（显示画布未贴任何 ROI 边、四边在 ROI 内完整）时：本切割对应路径 MUST NOT 强行发明切割边；SHOULD 走既有完整观测 / 直接工作区路径。

### 4.3 视口切割边

- 定义：视口（红框边）与**导航器缩略图显示画布**的交集中，真正**穿插 / 经过**显示画布的红边（段）；
- 可理解为「穿插在画布中的红线」。

### 4.4 非切割红边

- 定义：该视口边**没有任何部分**经过（与）导航器缩略图显示画布相交；
- MUST NOT 参与「谁切对应谁」；
- MAY 仍被观测/导出为红段，但 MUST NOT 被标成由切割对应得到的工作区边角色（除非由 §5.3 传播得到且与切割证据一致）。

判定：红边线段与缩略图显示画布（mask 或多边形）相交长度 / 像素 > 0 → 切割边；否则 → 非切割红边。

### 4.5 切割对应

一对 `(工作区切割边 e_w, 视口切割边 e_v)` 表示：**同一条裁切关系**在工作区侧与导航器视口侧的实现。

MUST NOT 解释为屏幕同侧同名（禁止默认 `Bottom↔Bottom`、`Top↔Top`）。  
例：工作区 **Bottom ROI** 切割时，对应的视口切割边往往是穿插缩略图显示画布的那条红线（图示 180° 场景下常为红框上沿）。

---

## 5. 对应算法（强约束）

### 5.1 主规则：谁切的就对应谁

```text
工作区切割边集合 C_w
视口切割边集合 C_v
→ 建立对应 π: C_w → C_v（谁切 ↔ 谁切）
```

MUST：

1. 仅在 `C_w` 与 `C_v` 的成员之间建立对应；
2. 非切割红边 MUST NOT 进入 `C_v`；
3. 对应语义为裁切同构，MUST NOT 为 ThumbnailRoi 内 coord 排序。

多对同时存在时：MUST 全部参与；若两对以上互相矛盾（同一 `e_w` 映到冲突角色，或旋转传播后不一致）→ MUST 失败为歧义（`AmbiguousViewportGeometry` 或等价状态），MUST NOT 静默挑一。

### 5.2 角的用途：仅逆向旋转加速（不得用于判边）

显示角（OCR 读数或既有可信角源，见 §6）：

- MUST 用于：将**工作区显示画布**（及必要时其切割边方向）按角做**逆向旋转**，使与缩略图显示约定对齐，从而加快贴合 / 切割匹配；
- MUST NOT 用于：单独给对称的两条视口边「猜」L/R 或 T/B 标签（判边权威是切割对应，不是角）。

四边标签在 0° 约定下固定；旋转后的角色由「切割对应 + 旋转群作用」得到，而不是「角直接投票边名」。

### 5.3 不足四对：一对传播四边

在 0° 约定下，上下左右与矩形四边固定绑定。

当已得到**至少一对**可靠切割对应，且显示角已知（用于确定相对 0° 的旋转类）时：

- MUST 可由该对对应传播得到其余视口边的 L/T/R/B；
- MUST NOT 再对未观测到的边使用 ROI 中线兜底作为主路径。

### 5.4 四对齐全：逆向旋转后直接套 L/T/R/B

当 `|C_w|=4` 且 `|C_v|=4` 且切割对应齐全时：

1. MUST 先对工作区显示画布做逆向旋转对齐；
2. MUST 再按 0° 约定直接套用左 / 上 / 右 / 下；
3. MUST NOT 再走「组内排序定角色」分支。

### 5.5 贴合与视口内部（辅助，非标签来源）

将逆向旋转后的工作区显示画布与「缩略图显示画布被视口切割后的区域」比对：

- 形状贴合部分 SHOULD 定义为**视口内部**，用于校验切割边朝向（哪一侧是内）；
- 边角色的**最终权威**仍是 §5.1 切割对应（+ §5.3/§5.4），MUST NOT 仅凭「贴合后的 AABB 谁更靠左」定案。

贴合实现 SHOULD 使用变换 + 边界/区域重合度量；MUST NOT 依赖全分辨率穷举角搜索作为默认路径。

---

## 6. 与 OCR / 几何角的边界

| 用途 | MUST / MUST NOT |
|------|-----------------|
| 逆向旋转以加速切割匹配 | MUST 允许使用可信角 |
| 单独作为边方位判决 | MUST NOT |
| 改写 Solve 中「几何旋转权威、OCR 诊断」的既有契约 | MUST NOT（除非另开求解契约修订） |
| OCR 角低置信 | SHOULD 降级：少量离散旋转候选（如 0/90/180/270）做切割匹配，或失败；MUST NOT 回退到 ROI 中线指派 |

---

## 7. 禁止事项（硬表）

实现 MUST NOT：

1. 用 ThumbnailRoi 中线作为 `workspace_edge` 的主判定；
2. 用组内「最左/最右/最上/最下」单独定案边角色（在切割对应路径启用时）；
3. 将非切割红边当作视口切割边参与对应；
4. 将「屏幕同侧同名」当作切割对应（禁止默认 Bottom↔Bottom）；
5. 在无工作区切割边时伪造切割边以强行跑本路径；
6. 多对切割对应冲突时静默选优打分（MUST 硬失败或硬消歧规则，禁止隐式排序获胜）；
7. 以「全图像素模板匹配 + 连续角穷举」作为默认实现（性能与稳定性违规风险）。

---

## 8. 对现有代码的强制修改点

### 8.1 `native/src/viewport_frame.cpp`（MUST）

- `AssignGroupWorkspaceEdges`（及单边中线改指派）在切割对应路径下 MUST 被替换或降级为：仅在本契约不适用时的遗留回退（若保留回退，MUST 在日志/结果中显式标记，且 SHOULD 逐步删除）。
- 边导出（`observed_red_edges` / `complete_edges` 的 `workspace_edge`）MUST 写入切割对应（或 §5.3/§5.4 传播）结果。

### 8.2 `WorkspaceCanvasRelation`（MUST 消费）

- `canvas_crop_sides` / `occluded_canvas_edges` / 可见 bounds 与比例 MUST 作为工作区切割边与对齐的输入证据，MUST NOT 继续只用于尺寸估计而忽略方位。

### 8.3 显示画布提取（MUST）

- 工作区与缩略图两侧 MUST 按 §4.1 得到显示画布（背景排除 + 弱几何修正仅约束背景规则）。
- 现有轴对齐 `ObserveCanvas` bounds MAY 复用为初值；切割边判定 MUST 能回答「是否贴 ROI 边 / 红边是否与显示画布相交」。

### 8.4 UI 覆盖层（MUST）

- `CompleteEdgeOverlayWindow` 的「左/右/上/下」MUST 继续只渲染 native 给出的 `workspace_edge`；C# MUST NOT 自行按屏幕位置改标签。

### 8.5 测试（MUST 覆盖）

1. **180° + 工作区底边切割**：工作区 Bottom 切割 ↔ 穿插缩略图显示画布的视口切割边（常见为红框上沿），标签不得标成「红框底边 = 下」的屏幕同侧错误；
2. **非切割红边**：完全不经过缩略图显示画布的红边不得进入切割对应；
3. **四对齐全**：逆向旋转后直接套 L/T/R/B，且与传播结果一致；
4. **一对传播**：仅一对切割对应 + 已知角 → 其余三边角色正确；
5. **冲突多对**：MUST 失败为歧义，不得静默成功；
6. **无工作区切割边**：不得误走切割对应主路径；
7. 回归：四边完整红框、理想 0° 场景既有契约测试不得无故破坏。

---

## 9. 失败语义

| 条件 | 行为 |
|------|------|
| 无法建立任何切割对应且本路径为求解所必需 | MUST 失败（视口几何不足/歧义） |
| 多对切割对应互相矛盾 | MUST `AmbiguousViewportGeometry`（或等价） |
| 角不可用且离散候选仍歧义 | MUST 失败；MUST NOT ROI 中线定案 |
| 直接工作区路径可用 | MUST NOT 因本契约失败而阻断直接路径 |

失败时 MUST NOT 自动改写 WorkspaceRoi / ThumbnailRoi 锚点。

---

## 10. 验收一句话

**视口边的左/上/右/下 = 工作区切割边与视口切割边的裁切同构（角只做逆向旋转加速）；不再等于缩略图 ROI 里谁更靠边。**

---

## 11. 文档关系

| 文档 | 关系 |
|------|------|
| `screen_canvas_transform/RECOMPUTE_PIPELINE_ARCHITECTURE.md` | 重算锚点与搜索域；本文不修改锚点冻结，只改 ThumbnailRoi **内**边角色 |
| `transfomer/SCREEN_CANVAS_TRANSFORM_ARCHITECTURE.md` | 总架构；本文为视口边方位的增量契约 |
| 本文 | `workspace_edge` / 切割对应的唯一强约束来源（本主题下） |

若与旧实现注释冲突，以**本文**为准，并 MUST 在落地 PR 中删除或改正冲突注释。
