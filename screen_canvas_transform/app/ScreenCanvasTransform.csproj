<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <PlatformTarget>x64</PlatformTarget>
    <Platforms>x64</Platforms>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <SelfContained>false</SelfContained>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <RootNamespace>ScreenCanvasTransform</RootNamespace>
    <AssemblyName>ScreenCanvasTransform</AssemblyName>
    <StartupObject>ScreenCanvasTransform.App</StartupObject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Drawing.Common" Version="8.0.11" />
  </ItemGroup>

  <ItemGroup>
    <None Include="Native\**\*" CopyToOutputDirectory="PreserveNewest" LinkBase="Native" Condition="Exists('Native')" />
  </ItemGroup>

  <Target Name="CopyNativeDll" AfterTargets="Build">
    <ItemGroup>
      <_NativeDll Include="$(MSBuildProjectDirectory)\Native\ScreenCanvasNative.dll" Condition="Exists('$(MSBuildProjectDirectory)\Native\ScreenCanvasNative.dll')" />
      <_NativeDll Include="$(MSBuildProjectDirectory)\..\native\build\ScreenCanvasNative.dll" Condition="Exists('$(MSBuildProjectDirectory)\..\native\build\ScreenCanvasNative.dll')" />
      <_NativeDll Include="$(MSBuildProjectDirectory)\..\native\build_src\ScreenCanvasNative.dll" Condition="Exists('$(MSBuildProjectDirectory)\..\native\build_src\ScreenCanvasNative.dll')" />
    </ItemGroup>
    <Copy SourceFiles="@(_NativeDll)" DestinationFolder="$(OutputPath)" SkipUnchangedFiles="true" Condition="'@(_NativeDll)' != ''" />
  </Target>

</Project>
